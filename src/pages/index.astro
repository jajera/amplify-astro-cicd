---

---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>amplify-astro-cicd</title>
    <style>
      :root {
        color-scheme: dark;
        --bg: #071014;
        --fg: #eef8fa;
        --muted: #b7cdd4;
        --accent: #22d3ee;
      }
      body {
        margin: 0;
        min-height: 100dvh;
        display: grid;
        place-content: center;
        gap: 0.75rem;
        padding: 2rem;
        font-family: Manrope, Segoe UI, sans-serif;
        background: var(--bg);
        color: var(--fg);
      }
      h1 {
        margin: 0;
        font-size: clamp(1.8rem, 4vw, 2.6rem);
        letter-spacing: -0.02em;
      }
      p {
        margin: 0;
        color: var(--muted);
        max-width: 36rem;
        line-height: 1.5;
      }
      a {
        color: var(--accent);
      }
    </style>
  </head>
  <body>
    <h1>amplify-astro-cicd</h1>
    <p>
      Minimal Astro sample for Amplify Hosting with staging, PR previews, and
      GitHub CI. Follow the
      <a href="https://amplify-astro-cicd-walkthrough.johna.kiwi/">walkthrough</a>.
    </p>
  </body>
</html>
